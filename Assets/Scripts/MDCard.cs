using UnityEngine;
using UnityEngine.UI;

namespace MDUI.Component
{
    public class MDCard : MonoBehaviour
    {
        public bool layoutPadding = true;

        // Use this for initialization
        void Start()
        {
            // apply(this.GetComponentInChildren<MDCardTitle>());
            // apply(this.GetComponentInChildren<MDCardSubTitle>());
            // apply(this.GetComponentInChildren<MDCardContent>());
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
